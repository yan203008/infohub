name: Collect InfoHub sources

permissions:
  contents: write
  pages: write
  id-token: write

on:
  workflow_dispatch:
    inputs:
      source:
        description: "只重跑一个来源；all 表示完整运行"
        required: true
        default: all
        type: choice
        options:
          - all
          - follow-builders
          - technical-x
          - papers
          - github
          - youtube
          - summaries
          - publish-only
  schedule:
    # UTC 17:17 = 北京时间次日 01:17；避开整点高峰，并为 10:00 前发布预留充足缓冲。
    - cron: "17 17 * * *"

concurrency:
  group: infohub-publisher
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 80
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Install transcript fallback
        if: inputs.source != 'publish-only'
        run: python -m pip install --disable-pip-version-check yt-dlp

      - name: Prepare YouTube transcript skill
        if: inputs.source != 'publish-only'
        run: git clone --depth 1 https://github.com/jimliu/baoyu-skills.git "$RUNNER_TEMP/baoyu-skills"

      - name: Collect and publish
        if: inputs.source != 'publish-only'
        id: collect_step
        continue-on-error: true
        env:
          MOONSHOT_API_KEY: ${{ secrets.MOONSHOT_API_KEY }}
          MOONSHOT_BASE_URL: https://api.kimi.com/coding/v1
          MOONSHOT_MODEL: k3-256k
          SUPADATA_API_KEY: ${{ secrets.SUPADATA_API_KEY }}
          GETNOTE_API_KEY: ${{ secrets.GETNOTE_API_KEY }}
          GETNOTE_CLIENT_ID: ${{ secrets.GETNOTE_CLIENT_ID }}
          BAOYU_TRANSCRIPT_SCRIPT: ${{ runner.temp }}/baoyu-skills/skills/baoyu-youtube-transcript/scripts/main.ts
        run: |
          if [ "${{ inputs.source }}" = "summaries" ]; then
            npm run summaries
          elif [ -n "${{ inputs.source }}" ] && [ "${{ inputs.source }}" != "all" ]; then
            npm run collect -- --source="${{ inputs.source }}"
          else
            npm run collect:reliable
          fi

      - name: Process scheduled curated links
        if: inputs.source != 'publish-only'
        id: curated_step
        continue-on-error: true
        env:
          MOONSHOT_API_KEY: ${{ secrets.MOONSHOT_API_KEY }}
          MOONSHOT_BASE_URL: https://api.kimi.com/coding/v1
          MOONSHOT_MODEL: k3-256k
          SUPADATA_API_KEY: ${{ secrets.SUPADATA_API_KEY }}
          GETNOTE_API_KEY: ${{ secrets.GETNOTE_API_KEY }}
          GETNOTE_CLIENT_ID: ${{ secrets.GETNOTE_CLIENT_ID }}
        run: npm run curated:queue

      - name: Sync tagged GetNote selections
        if: inputs.source != 'publish-only'
        id: getnote_sync_step
        continue-on-error: true
        env:
          MOONSHOT_API_KEY: ${{ secrets.MOONSHOT_API_KEY }}
          MOONSHOT_BASE_URL: https://api.kimi.com/coding/v1
          MOONSHOT_MODEL: k3-256k
          GETNOTE_API_KEY: ${{ secrets.GETNOTE_API_KEY }}
          GETNOTE_CLIENT_ID: ${{ secrets.GETNOTE_CLIENT_ID }}
          GETNOTE_SYNC_TAG: InfoHub精选
        run: npm run getnote:sync

      - name: Save new public content
        if: inputs.source != 'publish-only'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/generated-feed.json app/generated-section-summaries.json app/generated-run-summary.json public/generated-submission-status.json config/manual-queue.json
          if ! git diff --cached --quiet; then
            git commit -m "content: update daily digest"
            git push
          fi

      - name: Build GitHub Pages site
        run: npm run build:pages

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages site
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist-pages

      - name: Publish GitHub Pages site
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Keep collection report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: collection-report
          path: |
            outputs/last-collection*.json
            outputs/collection-status.json
          if-no-files-found: ignore
          retention-days: 14

      - name: Mark unresolved collection errors
        if: always() && (steps.collect_step.outcome == 'failure' || steps.curated_step.outcome == 'failure' || steps.getnote_sync_step.outcome == 'failure')
        run: |
          echo "采集结果已尽可能发布，但仍有来源或总结在重试后失败。请查看 collection-report。"
          exit 1
